package src.Events;

public class Config {
    public static String EVENTS_ZIP_FILE_PATH = System.getProperty("user.dir") + "/instructions/firewall.zip";
    public static String EVENTS_FILE_PATH = System.getProperty("user.dir") + "/instructions/firewall.log";
}
